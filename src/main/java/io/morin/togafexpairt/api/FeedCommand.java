package io.morin.togafexpairt.api;

import lombok.Builder;
import lombok.Value;

/**
 * The FeedCommand class is the command to feed the TOGAF Library.
 */
@Value
@Builder(toBuilder = true)
public class FeedCommand {}
